{
  "name": "tax-unified-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 8787",
    "test:e2e": "BASE_URL=${BASE_URL:-http://localhost:8787/tax-unified-platform} playwright test",
    "test": "npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0"
  }
}
