{
  "name": "tax-unified-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node ./node_modules/vite/bin/vite.js",
    "build": "node ./node_modules/vite/bin/vite.js build && npm run copy-static",
    "preview": "node ./node_modules/vite/bin/vite.js preview",
    "copy-static": "mkdir -p dist && cp -R yearend dist/ && cp -R corporate dist/ && cp -R financial dist/ && cp -R board dist/ && cp share.html dist/",
    "test:e2e": "BASE_URL=${BASE_URL:-http://localhost:4173} playwright test",
    "test": "npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "@vitejs/plugin-react": "^4.2.0",
    "copyfiles": "^2.4.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  }
}
